import 'package:flutter/material.dart';

class NotificationCustomerScreen extends StatelessWidget {
  const NotificationCustomerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text("NotificationCustomerScreen"),
      ],
    );
  }
}
