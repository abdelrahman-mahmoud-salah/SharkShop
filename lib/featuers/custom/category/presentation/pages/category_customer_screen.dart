import 'package:flutter/material.dart';

class CategoryCustomerScreen extends StatelessWidget {
  const CategoryCustomerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.expand(
      child: Text("Category Customer Screen"),
    );
  }
}
